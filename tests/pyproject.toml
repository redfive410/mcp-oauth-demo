[project]
name = "mcp-oauth-tests"
version = "0.1.0"
description = "Integration tests for MCP OAuth flows"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pyjwt[crypto]>=2.8.0",
    "cryptography>=42.0.0",
]
